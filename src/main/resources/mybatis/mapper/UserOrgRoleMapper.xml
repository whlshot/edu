<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ken.mapper.UserOrgRoleMapper">

    <insert id="saveUserOrgRole">
      insert into edu_user_org_role(user_id,org_id,role_id) values (#{userId},#{orgId},#{roleId})
    </insert>

</mapper>
